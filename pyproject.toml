[build-system]
requires = ["poetry>=1.0"]
build-backend = "poetry.masonry.api"

[tool.poetry]
name = "libsLinux"
version = "0.0.1"
authors = []
description = ""
packages = [ { include = "libsLinux" } ]

[tool.poetry.dependencies]
python = "^3.7, <3.8"
